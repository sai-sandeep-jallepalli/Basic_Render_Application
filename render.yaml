services:
  - type: web
    name: flask-app
    env: python
    rootDir: src
    buildCommand: pip install -r ../requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 5000
    plan: free